

<!DOCTYPE html>
<html lang="en">
<head>
   <%- include partials/_header.ejs %>
</head>
<body>
  <ul>
  <% for(var key in urls) { %>
    <li><%= key + ': ' + urls[key] %>
    <form action="urls/<%= key %>/delete"
    method="post">
        <a href="urls/<%= key %>">Edit</a>
        <button type="submit"> Delete</button>

        </form>
    </li>
    <% } %>


  </ul>

</body>
</html>

